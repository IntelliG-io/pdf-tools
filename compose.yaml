services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: pdf-tools-backend:local
    environment:
      - PYTHONPATH=/app
    ports:
      - "8000:8000"
    restart: unless-stopped
