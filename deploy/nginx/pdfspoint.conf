map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream pdf-tools-prod {
    server 127.0.0.1:9000;
}

upstream pdf-tools-dev {
    server 127.0.0.1:9001;
}

upstream pdf-tools-legacy {
    server 127.0.0.1:9002;
}

server {
    listen 80;
    server_name pdfspoint.com www.pdfspoint.com;

    location / {
        proxy_pass http://pdf-tools-prod;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name dev.pdfspoint.com;

    location / {
        proxy_pass http://pdf-tools-dev;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name legacy.pdfspoint.com;

    location / {
        proxy_pass http://pdf-tools-legacy;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
